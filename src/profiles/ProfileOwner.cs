public enum ProfileOwner
{
  /// <summary>
  /// The profile belongs to 10forward's own page content.
  /// </summary>
  Root,

  /// <summary>
  /// The profile belongs to a user.
  /// </summary>
  User,

  /// <summary>
  /// The profile belongs to an organization.
  /// </summary>
  Organization,
}
